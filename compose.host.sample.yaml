services:
  xboard:
    image: ghcr.io/cedar2025/xboard:latest
    restart: unless-stopped
    init: true                       # 让 Docker 以 tini 作 PID 1，回收 supervisord 下派生的 PHP 僵尸(defunct)进程
    network_mode: host
    volumes:
      - ./.env:/www/.env
      - ./.docker/.data/:/www/.docker/.data
      - ./storage/logs:/www/storage/logs
      - ./storage/theme:/www/storage/theme
      - ./plugins:/www/plugins
      - redis-data:/data
    environment:
      - RESOURCE_PROFILE=balanced      # minimal | balanced | performance | auto
      - ENABLE_HORIZON=true
      - docker=true

volumes:
  redis-data:
